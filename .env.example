# Copy this file to .env and fill in LOCAL_OPENAI_API_KEY with your key.
# The .env file is git-ignored so the key never lands in version control.
#
# Non-secret configuration (endpoint + model) is kept here and mirrored in
# the notebook code, so it is visible in the repository.

LOCAL_OPENAI_BASE_URL=https://litellm.vse.cz/v1
LOCAL_OPENAI_API_KEY=
LOCAL_MODEL_TEXT=qwen3.5:122b
